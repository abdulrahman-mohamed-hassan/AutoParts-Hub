name: tawsel
description: "Premium Car Parts Shop & Repair Support App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  path: ^1.9.0
  sqflite: ^2.4.1
  sqflite_common_ffi: ^2.4.0
  sqflite_common_ffi_web: ^1.1.1
  sqlite3_flutter_libs: ^0.5.42
  http: ^1.2.1
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  cloud_firestore: ^6.3.0
  
  # Animation dependencies
  page_transition: ^2.1.0
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true